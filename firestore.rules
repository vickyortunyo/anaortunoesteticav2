rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Precios: lectura pública (web), escritura solo usuarios autenticados (admin)
    match /prices/{priceId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
